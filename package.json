{
  "name": "icors",
  "version": "1.0.0",
  "description": "🤖 A basic Nodejs Serverless CORS Proxy.",
  "main": "./api/cors.js",
  "engines": {
    "node": "12.x"
  },
  "scripts": {
    "start": "npm run dev",
    "dev": "now dev",
    "pre-build": "now",
    "build": "now --prod"
  },
  "author": "ifyour",
  "license": "MIT",
  "eslintConfig": {
    "extends": "eslint-config-ifyour"
  },
  "dependencies": {
    "cors-anywhere": "^0.4.1"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "eslint-config-ifyour": "^1.0.6"
  }
}
